#!/bin/bash

set -euo pipefail

file=$(
    rg --line-number . ~/notes | fzf --delimiter : --preview "bat --color=always {1} --highlight-line {2}" | cut -d ':' -f 1 
)

bat --style=plain $file

#if [[ -n $file ]]; then
#    $EDITOR "$file"
#fi



